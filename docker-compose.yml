services:
  cron:
    build: .
    image: claude-morning
    volumes:
      - ./data:/root/.claude
      - .:/workspace
    command: daemon
    environment:
      - CLAUDE_MORNING_CRON_SCHEDULE=0 8 * * *
      - TZ=Asia/Bangkok
    restart: always
